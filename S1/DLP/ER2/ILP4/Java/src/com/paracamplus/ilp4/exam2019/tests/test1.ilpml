class Point {
    var x, y;
    method close() (print('close invoked'))
    method something() (if x=1 then print(x) else print("fermeture"))
}

autoclose p = new Point(1,1) in (
        p.something()
    )